# 怎么把改动更新到手机上

改完代码后，按顺序跑这三步。**顺序不能乱，第二步最容易忘。**

## 1. 打包网页

```bash
npm run build
```

把 `src/` 里的代码编译成浏览器能跑的文件，输出到 `dist/` 文件夹。

## 2. 同步进 Android 工程 ⚠️ 最容易忘的一步

```bash
npx cap sync android
```

把 `dist/` 里的东西**抄一份**到 `android/app/src/main/assets/public/`。

Android App 读的是抄过去的那一份，不是 `dist/`。**跳过这步的话，第三步打出来的 APK 里装的还是上次的旧网页** —— 代码明明改了，手机上却毫无变化，就是这个原因。

## 3. 打包成 APK

两种方式，任选其一。

### 方式 A：命令行（快，不用开 Android Studio）

```bash
cd android && JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew assembleDebug
```

约 35 秒。产物在：

```
android/app/build/outputs/apk/debug/app-debug.apk
```

**传之前先改个名字。** 每次打出来都叫 `app-debug.apk`，传到手机上分不清哪个是哪个，
还得自己重命名。改成看得出内容和日期的，比如 `我的文库-真人发音-20260901.apk`：

```bash
cp android/app/build/outputs/apk/debug/app-debug.apk "我的文库-真人发音-$(date +%Y%m%d).apk"
```

然后把改好名的文件传到手机上（微信传输助手 / USB / 网盘都行），点开安装即可。

> **为什么要写那一长串 `JAVA_HOME`？**
> 这台电脑没有把 Java 装进系统环境变量，直接敲 `java` 是找不到的。
> 但 Android Studio 自带了一个 Java，就在那个路径下。这句话的意思是「用 Android Studio 自带的那个 Java 来打包」。

### 方式 B：Android Studio（当初用的方式）

用 Android Studio 打开 `android/` 文件夹，点 Run（手机需 USB 连接并开启 USB 调试），或 Build → Build Bundle(s)/APK(s) → Build APK(s)。

Android Studio 内部会自己接上它自带的 Java，所以不需要配环境变量 —— 这就是当初只能靠它的原因。

---

## 当前状态备忘

| 项目 | 值 |
|---|---|
| 应用 ID（包名） | `com.yinghuoepoch.wordshelf`（2026-09-15 从 `com.colin.lyricvocab` 改的，上了 Google Play 就不能再改） |
| 应用名 | 中文系统显示「我的文库」，其他语言显示「WordShelf」（`res/values/strings.xml` 与 `values-zh/strings.xml`） |
| 版本 | versionCode `2` / versionName `1.1.0`（每次往 Play 传新包 versionCode 必须 +1） |
| 签名文件 | `android/keystore/upload.jks`，密码在 `android/keystore.properties`；两者都不进版本库，备份在 `D:/code/money/密钥备份/` |
| Android SDK | `C:\Users\qq208\AppData\Local\Android\Sdk` |
| Android Studio | `C:\Program Files\Android\Android Studio` |

## 打给 Google Play 的正式包

自己手机上装 debug 版就够了；**只有传 Google Play 才用这一节。** 前两步（`npm run build`、`npx cap sync android`）照旧，第三步换成：

```bash
cd android && JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew bundleRelease assembleRelease
```

产物两个：

```
android/app/build/outputs/bundle/release/app-release.aab   ← 传 Google Play 用这个
android/app/build/outputs/apk/release/app-release.apk      ← 装到手机上验证用这个
```

传之前改名，带版本和日期，比如 `WordShelf-1.1.0-release-20260915.aab`。

> **release 版和 debug 版是两个不同签名的包，不能互相覆盖安装。** 手机上装着 debug 版时装 release 版会提示签名不一致，要先卸载（先导出备份）。
> 包名改过之后，新包和旧的 `com.colin.lyricvocab` 是两个独立的 App，数据不通；用「导出备份 → 恢复」或坚果云同步把数据搬过去。

## 只想在电脑上看看效果

不用打包，直接：

```bash
npm run dev
```

浏览器打开 http://localhost:5173 。改代码会自动刷新，适合快速试效果。
